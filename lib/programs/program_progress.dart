import 'package:flutter/material.dart';

class ProgramProgress extends StatelessWidget {
  const ProgramProgress({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}