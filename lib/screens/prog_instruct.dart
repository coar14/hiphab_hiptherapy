import 'package:flutter/material.dart';

class ProgInstruct extends StatelessWidget {
  const ProgInstruct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}